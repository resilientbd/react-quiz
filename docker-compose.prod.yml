version: '3.8'

services:
  quizapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quizapp-production
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped